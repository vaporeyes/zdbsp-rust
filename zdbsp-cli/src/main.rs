// ABOUTME: zdbsp CLI entry point. Mirrors the C++ main.cpp argument surface from zdbsp.html.
// ABOUTME: Currently a stub; argument parsing and the per-map loop land in later phases.

fn main() -> std::process::ExitCode {
    eprintln!("zdbsp (Rust port): not implemented yet");
    std::process::ExitCode::from(2)
}
